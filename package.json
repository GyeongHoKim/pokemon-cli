{
  "name": "pokemon-cli-workspace",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Dev-tooling workspace for pokemon-cli (commit linting). Not published.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.7"
  }
}
